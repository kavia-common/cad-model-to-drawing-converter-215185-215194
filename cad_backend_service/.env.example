# Database URL. For Postgres use: postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=sqlite:///./app.db

# JWT configuration
JWT_SECRET=change-me
JWT_EXPIRE_MIN=60

# CORS
ALLOWED_ORIGINS=*

# Storage directory (files, outputs, previews)
STORAGE_DIR=./storage
